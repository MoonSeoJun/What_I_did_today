## ES2015 ~

```jsx
const num1 = 1;
const num2 = 2;
const result = 3;
const string = `${num1} + ${num2} = ${result}`;

console.log(string)
```

${변수} 형식으로 변수를 '+' 기호 없이 문자열에 넣을 수 있다

기본 따옴표 대신 **`** 을 사용하므로 **""** 나 **''** 와 함께 사용할 수도 있다.